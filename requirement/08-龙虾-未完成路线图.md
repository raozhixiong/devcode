# 08 - 龙虾未完成路线图（M2+）

> 状态基线：M1 内核已验收（2026-08-27，26 测试全过，分支 `feature/lobster-m1-kernel`）。
> **M2 内核补全已全部完成**（2026-08-27，46 测试全过，jar 启动验证通过）：真实 LLM 配置、权限 WS 弹窗、AGENTS.md 注入、doom loop、SSE 恢复、输入收件箱、task 子代理、上下文压缩、Plan 模式、writer claim 围栏、后台子代理 announce。
> **M3 多角色已全部完成**（2026-08-27，59 测试全过）：8 角色 RBAC + 工具过滤、队列模式 steer/followup/collect/interrupt、会话 fork/rewind/归档/标题、三层归属、状态感知。
> **M5 沙箱已完成**（2026-08-28）：SandboxService 模式 OFF/NON_MAIN/ALL + DockerBackend/LocalBackend + BashTool 接入。
> **M6 扩展内核已完成**（2026-08-28，188 测试全过）：钩子框架、命令注册、集成/OAuth、参考库、Artifacts、Computer 工具、Share/ACP、托管 worktree。
> **前端三栏工作台已完成**（2026-08-28）：三栏布局 + 类型感知工具卡片 + 会话树 + 斜杠/@/⌘K 命令面板。
> 本文对照 `03-龙虾-全量功能需求.md` 逐模块盘点差距，作为后续里程碑的实施依据。

## M1 已完成内容（勿重复实现）

| 模块 | 已交付 |
|---|---|
| Agent Loop | 状态机循环、MAX_STEPS=50 保护、中断占位 |
| Part 模型 | 8 种 Part sealed 接口、ToolState 4 态、delta/全量分离 |
| 工具系统 | Tool SPI + 9 内置工具（read/write/edit/glob/grep/bash/todo/question/ls）、权限包装、输出截断 |
| 权限引擎 | findLast 规则匹配（glob 双语义）、ALLOW/DENY/ASK、ask 挂起（30s 超时） |
| LLM Provider | LlmProvider 接口、OpenAiCompatProvider（SSE 流式）、MockLlmProvider |
| 事件总线 | durable（SQLite）+ live（WebSocket）双通道、断线重连回放 |
| 存储 | 共享库 18 表 + agent 库（M1 子集）、双 Flyway 迁移、WAL |
| WS 网关 | 帧协议 req/res/event、chat.send/chat.history/sessions.list、演示聊天页 |

---

## M2 内核补全

| # | 事项 | 状态 |
|---|---|---|
| 1 | 真实 LLM 接入（lobster.json llm 段，无配置回退 Mock） | ✅ 已完成 |
| 2 | 权限 ask 走 WS（permission.asked 事件 + permission.respond + 前端弹窗） | ✅ 已完成 |
| 3 | PromptAssembler（AGENTS.md 向上查找注入 + 前缀稳定 cache 边界） | ✅ 已完成 |
| 4 | 上下文压缩（TokenEstimator + compact 纪元 + 溢出 70% 自动压缩 + LLM summarizer 可插拔） | ✅ 已完成 |
| 5 | 子代理 task 工具（深度限制 2 层 + 子会话复用 loop） | ✅ 已完成 |
| 6 | 后台子代理 + announce（background_spawn 非阻塞 + 合成 user 消息注入 + 自动重触发） | ✅ 已完成 |
| 7 | Plan 模式（PlanMode 工具裁剪 + write 仅 plans/*.md + reminder 注入 + plan_exit + mode.set + 前端按钮） | ✅ 已完成 |
| 8 | 输入收件箱（busy 入队 + 轮结束 admit 合并） | ✅ 已完成（steer/followup 细分模式 M3） |
| 9 | writer claim 围栏（互斥 claim + 代际校验 + 孤儿清理 + loop 步进校验） | ✅ 已完成 |
| 10 | doom loop 检测（同参 3 次熔断） | ✅ 已完成 |
| 11 | 事件恢复 SSE（/api/session/{id}/events + stream） | ✅ 已完成 |

## M3 多角色

| # | 事项 | 状态 |
|---|---|---|
| 12 | 角色体系（Role 8 角色 + AgentRegistry CRUD + loop 工具过滤 + WS agents.*） | ✅ 已完成 |
| 13 | 队列模式（steer/followup/collect/interrupt + abort + queue.mode.set） | ✅ 已完成 |
| 14 | 会话管理（fork/rewind/归档/标题 + WS sessions.* + 软删除过滤） | ✅ 已完成 |
| 15 | 三层归属（Creator/Owner/Participants + ≤32 上限 + assignOwner） | ✅ 已完成 |
| 16 | 状态感知（stateVersion 乐观锁 + changesSince + session.state/changesSince） | ✅ 已完成 |

## M4 流程平台

| # | 事项 | 说明 |
|---|---|---|
| 17 | 任务台账 | ✅ 已完成（TaskStore + tasks.list/get/cancel RPC + BackgroundSpawnTool 集成） |
| 18 | Workboard | ✅ 已完成（9 列看板、卡片 CRUD/move/事件历史 + workboard.cards.* RPC） |
| 19 | Cron 调度 | ✅ 已完成（heartbeat 定时 + cron 表达式触发会话 + cron.* RPC） |
| 20 | 五层记忆 | ✅ 已完成（episodic 写入 + memory_search 工具 + Dreaming sweep + provenance 闸门） |
| 21 | Usage 统计 | ✅ 已完成（UsageStore + usage.byAgent/session/daily/sessions RPC） |
| 22 | Skills | ✅ 已完成（SkillsStore + skills.list/get/setEnabled/install RPC + SKILLS_CHANGED 事件）+ **SkillTool 已实现并注入 prompt 技能清单、触发 skill.completed 钩子** |
| 22c | MCP 客户端 | ✅ 已完成（McpClient JSON-RPC stdio + McpManager + McpTool/McpResourceTool 包装 + lobster.json `mcp.servers` 配置 + 触发 mcp.tool.completed 钩子） |
| 22a | 集成/OAuth 框架 | ✅ 已完成（IntegrationStore + integration.connect.key/oauth + attempt 状态机 + integration.* RPC + V4 迁移，FR-I4） |
| 22b | 参考库 References | ✅ 已完成（ReferenceStore + reference.install/setEnabled/remove RPC + V5 迁移 + prompt 注入 + REFERENCE_CHANGED 事件，FR-I3） |

## M5 企业化

| # | 事项 | 说明 |
|---|---|---|
| 23 | 频道接入 | webhook 通用、企微/钉钉/飞书适配器 |
| 24 | 审批中心 | exec 审批流、审批列表 UI |
| 25 | 沙箱 | ✅ 已完成（SandboxService 模式 OFF/NON_MAIN/ALL + DockerBackend/LocalBackend + BashTool 接入，FR-M5-25） |
| 26 | 审计台账 | 敏感操作审计记录与查询 |
| 27 | 认证 | 设备配对、WS 鉴权（当前免鉴权） |
| 28 | 配置中心 | schema 驱动设置页、插件系统（含 hook/reference/command 贡献点，FR-I5） |

## M6 扩展内核（新增，对齐 opencode/opencode 插件与生命周期体系）

| # | 事项 | 说明 |
|---|---|---|
| 29 | 通用钩子框架 | ✅ 核心已完成（HookStore + HookEngine + hooks.install/list/setEnabled/remove RPC + V3 迁移 + AgentLoop 接入 run.started/run.ended/tool.before/tool.after/skill.completed/mcp.tool.completed，block/after 语义，global/agent/session 作用域，与 FR-A4-8、FR-H-5 归并，FR-I1） |
| 30 | 命令注册体系 | ✅ 已完成（CommandRegistry + command.list RPC + workspace `.lobster/commands` 加载 + 前端 ⌘K 面板，FR-I2） |
| 31 | 工件与多模态 | ✅ 已完成（ArtifactsStore + artifact.* RPC + V6 迁移 + ComputerTool/LocalComputerBackend 截图/导航/点击/输入 + Promised Work 守卫沿用 PermissionEngine，FR-I6） |
| 32 | 会话分享 / ACP | ✅ 已完成（ShareService + share.create/open RPC + V7 迁移 + AcpController `/acp/v1/*` 最小实现，FR-I7） |
| 33 | 托管 worktree 自动化 | ✅ 已完成（WorktreeService + worktree.create RPC + git worktree 隔离分支，FR-I8） |

## 前端（对照 `04-龙虾-原型设计.md`）

| # | 事项 | 说明 |
|---|---|---|
| 29 | 三栏工作台 | ✅ 已完成（左栏会话树、中栏消息流、右栏参考库/集成/产物面板） |
| 30 | 工具卡片 | ✅ 已完成（类型感知：可折叠详情、diff +/- 着色、错误态） |
| 31 | 会话树 | ✅ 已完成（侧边栏会话切换 + 新会话） |
| 32 | 输入增强 | ✅ 已完成（斜杠命令、@补全、⌘K 命令面板） |

## 建议节奏

- **M4/M5-沙箱/M6/前端均已完成**（2026-08-28，188 测试全过）。
- **M5 尚余**：频道接入（webhook/企微/钉钉/飞书，部分 webhook 已有）、审批中心、审计台账、认证（WS 鉴权）、配置中心 UI（配置中心/插件后端已交付）。
- 每个里程碑单独建 feature 分支 + 计划文档（`docs/superpowers/plans/`），验收标准 = 全量 `mvn test` 通过 + 端到端手工验收
